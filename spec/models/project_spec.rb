# frozen_string_literal: true

require 'rails_helper'

RSpec.describe Project do
  context 'Associations' do
    # it { is_expected.to have_many(:task).optional(true) }
    # it { is_expected.to belong_to(:user).required(true) }
  end
end
